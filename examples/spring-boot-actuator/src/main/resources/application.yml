spring:
  application:
    name: spring-boot-actuator-regression

server:
  servlet:
    context-path: /v1

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
